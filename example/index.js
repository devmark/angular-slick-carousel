'use strict';

angular.module('slickExampleApp', ['bardo.directives'])
  .controller('SlickCtrl', function ($scope) {
    $scope.slickConfig = {
      dots: true
    };

    $scope.slickHandle = {

    };
  });
